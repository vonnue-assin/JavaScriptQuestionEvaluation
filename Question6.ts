// 6. Develop a string compression tool.

// Input: "aaabbbbcc", "encrypt"
// Output: "a3b4c2"

// Input: "a3b4c2", "decrypt"
// Output: "aaabbbbcc"

export const compressionTool = (value: string) => {
  let input = "";
  let output = "";
  for (let i = 0; i < value.length; i++) {
    let itemCount = value.repeat;
  }
};
